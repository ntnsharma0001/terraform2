resource "aws_instance" "name" {
  
}
# "   terraform import aws_instance.name i-09215212165054ff3   "
# above command is use to start tracking the existing resource inside the aws account. after create this type of block 
# you will get tf state file inside that file you will get all the detils /congiguration of that particular resource.
# then acc. to you you can modify the resource.buy giveing your details 
